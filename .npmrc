# Use the node-modules linker (default behavior in npm)
# No explicit configuration is needed for this

# Configure authentication for GitHub Package Registry
//npm.pkg.github.com/:_authToken=${NPM_TOKEN}

# Configure scope for @jaclight to use the GitHub Package Registry
@jaclight:registry=https://npm.pkg.github.com
